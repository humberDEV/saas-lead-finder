/** Clases compartidas del shell autenticado (sidebar vs contenido principal) */

export const appMainClass =
  "max-w-6xl mx-auto w-full px-5 sm:px-8 md:px-10 py-6 md:py-9 pb-24";

/** Scroll interior del canvas (el fondo lo pone AppShell) */
export const appScrollClass = "h-full overflow-y-auto";
